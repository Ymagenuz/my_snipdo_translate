"""Retired legacy entry point.

The former Google-based prototype embedded an obsolete credential and has been
removed from the current working tree.  It is intentionally not packaged.
Use ``gemini_translate.pyw`` or the Windows ``SnipDoTranslate.exe`` instead.
"""

raise SystemExit("This legacy prototype has been retired.")
